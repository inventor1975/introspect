# code2py — ночной прогресс (2026-09-11 → утро), Логик1 за главного

## На момент ухода куратора спать (21:00)
6 срезов + кроссфайл. 15 фикстур зелёные. scratchpad/code2py.
Возможности: тайнт F/T/Z, сводки межфункц., guards(whitelist), объекты по получателю (коллизия .execute погашена), точки входа (views), кроссфайловые сводки (вью→DAO→сток).
Калибровка: microblog 0 ложных (идиоматичный, правильно); dvpwa 0 (запросы параметризованы + был однофайловый — теперь кроссфайл есть).

## Сделано ночью (дописываю по ходу):

### Кроссфайл + чистка вывода (21:xx)
- analyze_app: индексирую сводки по ВСЕМ файлам, потом сужу — вью→DAO→сток через файлы ловится (фикстура xfile зелёная).
- Хожу по ВСЕМ телам функций (параметры чистые F): внутренний источник→сток и кроссфайл-вызовы ловятся; лишний EARNED от param-стоков подавлен (вывод = только REFUTED/OPEN из тел).

### ДЕТЕКЦИЯ на реальном уязвимом коде — DSVW (98 строк, сырые дыры)
code2py пометил ВСЕ 9 уязвимых стоков DSVW как OPEN: cursor.execute(SQLi ×3), pickle.loads(deser), subprocess.run shell=True(RCE), open(path-traversal ×3), exec(code). OPEN а не REFUTED — потому что ИСТОЧНИК DSVW (свой разбор query у http.server) не в каталоге → честное «не знаю, проверь». Ни одной не пропустил, ни одной не переобвинил. Третий ответ зеро-траста работает на реальном коде.

### ДЕТЕКЦИЯ на реальном Django — pygoat (уязвимое приложение, 80 файлов)
3 REFUTED (все ПРОВЕРЕНЫ, настоящие) + 14 OPEN + 2 EARNED:
- views.py:460 eval(request.POST.get('val')) — инъекция кода ✓
- mitre.py:218 eval(request.POST.get('expression')) — инъекция кода ✓
- mitre.py:242 command_out("nmap "+request.POST['ip']) — КРОССФУНКЦИОНАЛЬНАЯ инъекция команды ✓
Django-источники (request.POST.get) распознаны; кроссфункция работает; ложных REFUTED нет.
OPEN (14): pickle.loads/yaml.load/subprocess/open — опасные стоки с непрослеженным источником → честное «проверь».

ИТОГ ДЕТЕКЦИИ: на уязвимых (pygoat, DSVW) — ловит настоящие дыры (REFUTED уверенно + OPEN честно); на чистых (microblog) — тихо, 0 ложных. Порт доказан НА РЕАЛЬНОМ КОДЕ, не только фикстурах.

### Калибровка по приложениям (кроссфайл)
| приложение | тип | вердикты |
|---|---|---|
| pygoat | уязвимый | 3 REFUTED (все настоящие) + 14 OPEN |
| DSVW | уязвимый | 9 OPEN (все уязвимые стоки) |
| microblog | обычный | 1 OPEN, 0 REFUTED |
| flask | обычный | 18 OPEN, 0 REFUTED |
| dvpwa | aiohttp | 0 (фреймворк не подключён) |

ГЛАВНОЕ: 0 ложных обвинений (REFUTED) на чистом коде; настоящие дыры пойманы на уязвимом. OPEN = честное «не знаю» на непрослеженных стоках.
Пробел: dvpwa (aiohttp — источник не довязан + запросы параметризованы). Шум OPEN на open()/file — присущ файловым стокам (путь не прослежен → честный OPEN).

## ЧЕРНОВИК УТРЕННЕГО ДОКЛАДА
За ночь: code2py дорос до самотестируемого модуля (7 срезов, 15 фикстур + кроссфайл, гейт зелёный). Возможности: тайнт F/T/Z, межфункц. сводки, guards, объекты по получателю (коллизия имён погашена), точки входа, КРОССФАЙЛ.
ДОКАЗАНО НА РЕАЛЬНОМ КОДЕ: на pygoat (Django) поймал 3 НАСТОЯЩИЕ дыры (eval-инъекция ×2 + кроссфункц. инъекция команды), на DSVW — все 9 стоков; на чистых (microblog/flask) — 0 ложных обвинений. Порт зеро-траста на питон РАБОТАЕТ.
Пробелы (дорога): ширина фреймворков (aiohttp/fastapi), точность файловых стоков, оформление в папку python/.

### Триаж чистого кода (flask 18 OPEN) — ПОДТВЕРЖДЕНО честными, не FP
Все 18 OPEN = честное «не знаю» на непрослеженных аргументах стоков: open ×11 (config.py/app.py/cli.py — пути из конфига/ресурсов, НЕ атакующего), executescript (schema.sql), внутренние eval/compile. Ни одного ложного REFUTED. Прибор не ЗАЯВЛЯЕТ «безопасно» там, где не проследил — зеро-траст честно. Файловые стоки дают базовый OPEN-фон на любом коде (путь не прослежен).

### ПОЗДНЯЯ НОЧЬ: async + контейнеры + внутрифункц. dataflow + прямой ClassName.method
Довязал: taint контейнеров (Dict/List/%-форматирование), сводки теперь ХОДЯТ по телу (param→local→сток, не только напрямую), прямой ClassName.method() (staticmethod, offset 0) + instance (offset 1), поддержка async def (aiohttp/FastAPI).
РЕЗУЛЬТАТ — dvpwa пойман: views.py:57 REFUTED [sql] Student.create()->sink — КРОССФАЙЛОВАЯ %-форматированная SQLi в ASYNC aiohttp (data=await request.post() → data['name'] → Student.create → q="..%(name)s.."%{..} → cur.execute). Одна, настоящая; параметризованные DAO НЕ тронуты.

## ИТОГОВАЯ калибровка (все ночные фиксы)
| приложение | тип | вердикты | REFUTED (все проверены настоящими) |
|---|---|---|---|
| pygoat (Django) | уязвимый | 3 REFUTED, 14 OPEN | eval×2 + кроссфункц. команда |
| dvpwa (aiohttp) | уязвимый | 1 REFUTED | кроссфайл async %-SQLi |
| DSVW (http.server) | уязвимый | 9 OPEN | все стоки (источник http.server не в каталоге→честный OPEN) |
| microblog | обычный | 0 REFUTED, 1 OPEN | — |
| flask | обычный | 0 REFUTED, 18 OPEN | — |

## УТРЕННИЙ ДОКЛАД (готов)
За ночь code2py стал реальным многофреймворковым анализатором питона. Самотест зелёный (15 фикстур+кроссфайл). Возможности: F/T/Z, межфункц.+ВНУТРИфункц. сводки, guards, объекты по получателю, точки входа, КРОССФАЙЛ, контейнеры, ASYNC, прямой ClassName.method.
ДОКАЗАНО НА РЕАЛЬНОМ КОДЕ через 3 фреймворка: Flask(pygoat) 3 настоящие дыры, aiohttp(dvpwa) кроссфайл async SQLi, http.server(DSVW) 9 стоков; на чистом (microblog/flask) 0 ложных обвинений. Порт зеро-траста на питон работает.
Модуль: ZTL/tool/code2py/ (untracked, НЕ пушил). Дорога: точность файловых стоков, FastAPI/Django-ORM, оформить python/ по твоей структуре, СПЕЦ семантики как общий контракт php↔python.

### ГЛУБОКАЯ НОЧЬ: точность потока (transparent transformers + вложенные блоки)
Триаж pygoat OPEN вскрыл 4 реальных пробела, все закрыл:
- Прозрачные преобразователи (b64decode/decode/encode/str/.get/.read/.strip) СОХРАНЯЮТ тайнт (не санитайзеры).
- str.format(args) пробрасывает тайнт (как %).
- Django-источники request.FILES/POST/body/META (верхний регистр).
- _stmts РЕКУРСИВНО заходит в try/for/while/with (вложенные присваивания теперь прослеживаются) — раньше только if.
РЕЗУЛЬТАТ (проверено вручную): pygoat 3→5 REFUTED (добавились yaml.load(request.FILES) — Django YAML-RCE; pickle.loads(b64decode(request.form)) — Flask object-injection, «intentionally vulnerable»), flask OPEN 18→14 (шум ↓), чистые всё ещё 0 ложных REFUTED.

## ИТОГ (глубокая ночь) — многофреймворковый анализатор питона, доказан на реальном коде
| приложение | тип | REFUTED (все проверены) | OPEN |
|---|---|---|---|
| pygoat (Django/Flask) | уязвимый | 5 (eval×2, команда, yaml-RCE, pickle-RCE) | 10 |
| dvpwa (aiohttp) | уязвимый | 1 (кроссфайл async %-SQLi) | 0 |
| DSVW (http.server) | уязвимый | (9 OPEN — источник http.server не в каталоге) | 9 |
| microblog | обычный | 0 | 1 |
| flask | обычный | 0 | 14 |
Гейт зелёный (15 фикстур+кроссфайл). Модуль ZTL/tool/code2py/ (untracked, НЕ пушил).

### +re.sub transparency: pygoat 6 REFUTED (все настоящие)
Добавил re.sub/subn прозрачными на строке-аргументе. views.py:430 → REFUTED: subprocess.Popen("dig "+re.sub(...request.POST['domain']...), shell=True) — настоящая инъекция команды. Итог pygoat: 6 REFUTED (eval×2, команда×2, yaml-RCE, pickle-RCE), 9 OPEN. Гейт зелёный.

## ИЗВЕСТНОЕ ОГРАНИЧЕНИЕ (главное к дневной работе): ветвление if/else
_stmts ФЛЭТит ветки в один env последовательно → присваивание в if-ветке может затереть else-ветку. Может дать И промах (pickle views.py:214 остался OPEN — if-ветка token=чистый затирает else token=b64decode(cookie)) И потенциально ложный REFUTED. НУЖЕН правильный branch-join (ветки независимо + слияние taint=join). Это аккуратная дневная переделка _walk, не ночью. Пока: на чистых 0 ложных REFUTED (не проявилось), но риск есть.

## ФИНАЛ НОЧИ — сводка
code2py: многофреймворковый анализатор питона, самотест зелёный. Реальная детекция: pygoat 6, dvpwa 1 (все REFUTED проверены настоящими); DSVW 9 OPEN; чистые 0 ложных. Модуль ZTL/tool/code2py/ (untracked, НЕ пушил). Дорога: branch-join (главное), файловые стоки, FastAPI, оформить python/, СПЕЦ семантики php↔python.

### BRANCH-JOIN РЕШЁН (главное ограничение закрыто)
Переписал _walk структурно: if/else считаются в ОТДЕЛЬНЫХ env, потом СЛИЯНИЕ (taint=join). Ветка больше не затирает соседнюю. Try/for/while/with — рекурсивно; sinks в условиях/итераторах тоже ловятся.
Проверено: pygoat 6→7 REFUTED (views.py:214 pickle.loads(b64(request.COOKIES)) — object-injection, теперь ловится); НА ЧИСТОМ 0 ложных REFUTED (branch-handling НЕ дал ложняков); гейт зелёный (15+кроссфайл). Ограничение из прошлого пункта СНЯТО.
Итог: pygoat 7 наст. REFUTED, dvpwa 1, чистые 0 ложных. code2py: корректное ветвление + всё прежнее.

### Общий СПЕЦ семантики написан (модульный контракт)
tool/INTROSPECT-SEMANTICS.md — контракт для ВСЕХ языковых модулей: вердикты F/T/Z + зеро-траст OPEN, схема каталога, правила потока (сводки меж+внутрифункц.+кроссфайл, guards, receiver-aware объекты, transparent transformers, точки входа, branch-join). Общее = спец+судья ZFL+схема+тесты; пер-модуль = AST-ходилка языка + данные каталога экосистемы. Это «делим дёшёвое, не код-ядро» из договорённости с куратором. Ссылки на референсы: code2zfl(PHP), code2py(Python).

### +FastAPI/Starlette источники (4-й фреймворк)
Добавил request.query_params/path_params + методы form/body (Starlette/FastAPI Request). Фикстура v2_fastapi зелёная (request.query_params→os.system=REFUTED). Регрессии нет: pygoat 7, dvpwa 1, чистые 0. Теперь 4 фреймворка: Flask, Django, aiohttp, FastAPI. Гейт: 16 фикстур+кроссфайл.
