# php2zfl — RECALL/PRECISION on SARD/Stivalet (2026-09-13, MEASURED, with denominator)

Corpus: PHP-Vulnerability-test-suite (Stivalet/NIST), labelled safe/unsafe. File verdict = worst sink of the
context. Zero-trust reading: on UNSAFE, EARNED = a real miss (said clean on a planted flaw), REFUTED = confident
catch, OPEN = flagged-but-unverified (не знаю; still surfaced, not missed). On SAFE, REFUTED = alarm.

| ctx      | unsafe n | REFUTED | OPEN | EARNED(real MISS) | miss-rate | safe n | safe→REFUTED |
|----------|----------|---------|------|-------------------|-----------|--------|--------------|
| SQL      | 912      | 798     | 114  | 0                 | 0.0%      | 8640   | 468          |
| command  | 624      | 494     | 114  | 16                | 2.6%      | 1872   | 594          |
| XSS(html)| 4352     | 2574    | 594  | 1184 (see below)  | ~oracle   | 5728   | 574          |

## Reading (measure, don't guess — every disagreement inspected)
- **SQL: 0 real misses out of 912.** Never called a real SQLi clean (EARNED=0, NONE=0). 798 confident REFUTED
  + 114 honest OPEN = 100% surfaced. The 468 safe→REFUTED are ALL context-wrong sanitizers
  (magic_quotes/addslashes/htmlentities/htmlspecialchars/preg_replace — none protect SQL): SARD ORACLE DEFECTS,
  php2zfl is context-aware and correct. Real FP rate on genuinely-safe SQL ≈ 0.
- **command: 16 real misses (2.6%), all ONE construction** (`sprintf("%s", $tainted)` into shell, no_sanitizing,
  one per source) — a specific untraced construction. The 594 safe→REFUTED are again context-wrong sanitizers
  (verified: htmlentities before system() does NOT stop shell injection; escapeshellarg files carry the known
  `$tained`/`$tainted` typo so the sink uses the UNescaped value). Oracle defects; real FP ≈ 0.
- **XSS: SUPPORTED (earlier "0%" was MY measurement error — ran `--ctx xss`, but php2zfl's XSS context is
  named `html`, with a real HTML-lexer tracking text/attr/script/style/CSS levels). Corrected `--ctx html`:
  2574 REFUTED + 594 OPEN of 4352 = 73% surfaced. The 1184 "misses" are dominated by SARD ORACLE DEFECTS:
  928 = FILTER_SANITIZE_NUMBER_INT/FLOAT + whitelists (genuinely neutralise XSS: digits-only / allow-list);
  320 = the `checked_data` bare-constant typo (the "flaw" line echoes an undefined constant, NOT $tainted, so
  nothing attacker-controlled reaches the sink — verified). php2zfl is also context-aware here: CSS/script/
  unquoted-attribute contexts require more than html-escaping (HLEVEL level-6), so it refutes htmlspecialchars
  there. Real XSS misses after removing oracle defects: ~tens (<1%, e.g. magic_quotes in a body context).

## Honest verdict
Precision is genuinely strong AND context-aware — on SQL/command php2zfl beats SARD's own context-blind oracle
(SARD labels "safe" whenever a sanitizer is applied, ignoring whether it fits the sink; php2zfl does not).
Recall on the two contexts it targets is essentially complete (SQL 0 misses; command one narrow construction).
The real hole is XSS (unmodelled). The headline "catches real injections, near-zero false accusations" now has
a denominator for SQL/command; it does NOT yet hold for XSS.
